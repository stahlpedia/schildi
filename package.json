{
  "name": "schildi-dashboard",
  "version": "1.0.0",
  "description": "Dashboard für die Zusammenarbeit zwischen Thomas und Schildi 🐢",
  "scripts": {
    "dev": "node server/index.js",
    "build": "cd client && npm run build",
    "start": "NODE_ENV=production node server/index.js",
    "setup": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "marked": "^15.0.0",
    "multer": "^2.0.2",
    "node-fetch": "^2.7.0"
  }
}
